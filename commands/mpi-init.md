---
name: mpi-init
description: Bootstrap the kanban board, optionally importing entries from a freeform to-do / backlog / ideas markdown file.
---

Invoke the `mpi-init` skill and follow its checklist. If the user provided a
source file path or pasted file contents, parse it per the skill's rules and
preview the planned entries BEFORE writing anything to the kanban.
