---
name: mpi-brainstorm
description: Collaboratively explore an idea through dialogue, capture it as a BACKLOG entry on the kanban, then optionally hand off to /mpi-write-plan.
---

Invoke the `mpi-brainstorm` skill and follow its checklist. Do not skip the
design-approval gate or the BACKLOG entry capture.
