---
name: mpi-write-plan
description: Decompose a complex goal into a structured plan file (parallel investigation, sequential execution) and move the matching kanban entry to PLANNING.
---

Invoke the `mpi-write-plan` skill and follow its workflow end-to-end. Make
sure the kanban update step runs after the plan file is written — never leave
the board out of sync.
