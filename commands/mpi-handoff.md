---
name: mpi-handoff
description: Generate a structured JSON handoff document so a fresh session can resume work immediately.
---

Invoke the `mpi-handoff` skill and follow it end-to-end. Print only the resume
prompt block to the user — never dump the full JSON to chat.
