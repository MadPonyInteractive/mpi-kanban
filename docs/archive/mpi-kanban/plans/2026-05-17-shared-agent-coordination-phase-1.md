# Shared Claude/Codex Coordination Contract - Phase 1

## Current State

MPI Kanban is currently a Claude Code plugin with a Codex bridge. The human
board lives at `.claude/mpi-kanban/kanban.md` and is rendered by the companion
VS Code extension. The agreed coordination direction is captured in
`docs/brainstorms/2026-05-17-agent-coordination.md`.

Phase 1 now makes Claude and Codex read the same shared coordination contract
without yet automating the full claim lifecycle. The canonical machine state
location is `.agents/mpi-kanban/state/`; the existing board path remains
unchanged for extension compatibility.

Local implementation, validation, and the final live-update/polish pass are
complete. `update_live.py` copied the checkout to the Claude plugin cache for
version `0.4.1`.

## Implementation

- [x] Implement the Phase 1 shared coordination contract locally. **Verify:** docs/spec/bridge files consistently describe `.agents/mpi-kanban/state/`, the board remains at `.claude/mpi-kanban/kanban.md`, README install/test notes are current, and targeted searches show no contradictory canonical-state wording.
- [x] Run the final live-update/polish pass before Phase 2. **Verify:** `update_live.py` is intentionally run when active projects can tolerate the plugin cache refresh, then rerun targeted validation/search checks against the final live copy.

## Scope

Phase 1 includes:

- Update `SPEC.md` with the accepted shared Claude/Codex coordination model.
- Update `PLAN.md` with Phase 1 completion criteria and Phase 2 follow-up.
- Add shared coordination reference docs for:
  - state directory layout,
  - minimal JSON schemas,
  - lightweight role permissions,
  - UUID helper usage,
  - handoff migration behavior.
- Add or update helper tooling for UUID generation.
- Update Claude-facing skill docs to point at the shared contract where needed.
- Update Codex-facing `AGENTS.md` bridge instructions so Codex has equal access
  to the same contract.
- Update README installation/testing notes for the new shared `.agents/`
  contract.
- Preserve compatibility with the current VS Code extension and board path.

Phase 1 does not include:

- Full automated claim lifecycle.
- Reviewer/verifier/integrator automation.
- Stale claim reclaim automation.
- Coordination-state cleanup automation.
- VS Code extension visualization changes.

## Accepted Decisions To Encode

- Human board remains `.claude/mpi-kanban/kanban.md`.
- Canonical machine coordination state is `.agents/mpi-kanban/state/`.
- New canonical handoffs live under `.agents/mpi-kanban/state/handoffs/`.
- `docs/handoffs/` is legacy compatibility during migration.
- Core records use UUIDs as primary ids.
- UUIDs should be generated by shared helper tooling.
- `state/index.json` is the small facade every agent reads first.
- `index.json` directly lists active file paths and claim/state pointers.
- Default heartbeat timeout is 2 hours.
- Stale claims are reclaimable by an orchestrator or integrator; uncertain
  cases ask the user.
- Roles are lightweight behavior contracts: orchestrator, planner,
  implementer, reviewer, verifier, integrator, docs.
- Session records include explicit `allowed_actions`.
- Reviewer is first-class and read/review by default; it does not take write
  ownership unless explicitly reassigned.
- User-owned/manual kanban tasks stay board-only unless agent coordination is
  needed.
- Active records retain only a short recent history window, roughly 5-10 events.
- Phase 2 makes `mpi-cleanup` own conservative coordination-state garbage
  collection.

## Minimal Schemas To Define

Define examples for:

- `state/index.json`
- `state/sessions/<uuid>.json`
- `state/files/<uuid>.json`
- `state/tasks/<uuid>.json`
- `state/handoffs/<uuid>.json`

The schemas should stay compact and pointer-driven so agents can inspect active
coordination state without loading unnecessary history.

## Verification

Before Phase 1 is considered complete:

1. Check docs for contradictory canonical-state paths.
2. Check skills and bridges consistently point Claude and Codex at the shared
   contract.
3. Check README explains install/update and testing expectations.
4. Verify the board contract still uses only the locked columns and metadata.
5. Run the repository's available validation checks, if present.
6. Test the installed Claude plugin update flow before starting Phase 2.

## Completed

- [x] Added shared coordination reference docs under `docs/coordination/`.
- [x] Added `scripts/new_uuid.py` for UUIDv4 coordination record IDs.
- [x] Updated `SPEC.md`, `PLAN.md`, `README.md`, `AGENTS.md`, and `CLAUDE.md`.
- [x] Updated `mpi-handoff`, `mpi-continue`, and `mpi-cleanup` skill docs for
  canonical `.agents/mpi-kanban/state/` behavior.
- [x] Verified plugin validation, UUID helper, targeted canonical-state
  searches, and board contract compatibility.
- [x] Ran an initial `update_live.py` copy to plugin cache version `0.4.1`.
- [x] Preserved the canonical coordination path in shared plugin-development
  memory.

## Remaining Work

- Phase 1 is complete.
- Next plan: Phase 2 automation for shared agent coordination lifecycle.

## Plan Drift

- None yet.

## Preservation Notes

- Keep `docs/brainstorms/2026-05-17-agent-coordination.md` as the design record.
- Project memory was updated for the canonical coordination path.
- Do not edit the VS Code extension during Phase 1 unless compatibility breaks.
- Final live update was explicitly approved after confirming active projects can
  tolerate the plugin cache update.
